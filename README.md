
# API SQL con Express y CORS

Para la creación de la API se ha usado Node con el modulo express y cors. 

Para iniciar correctamente la api es necesario ejecutar los siguientes comandos:

`npm i express`

`npm i cors`

`node index.js`
 

